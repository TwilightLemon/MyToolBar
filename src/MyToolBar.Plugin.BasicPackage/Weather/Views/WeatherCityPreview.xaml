<base:ItemBase x:Class="MyToolBar.Plugin.BasicPackage.Weather.Views.WeatherCityPreview"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:base="clr-namespace:MyToolBar.Common.UIBases;assembly=MyToolBar.Common"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:local="clr-namespace:MyToolBar.Plugin.BasicPackage.Weather.Views"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:ws="https://schemas.elecho.dev/wpfsuite"
               Width="330"
               Height="120"
               Command="{Binding DataContext.SelectCityCommand, RelativeSource={RelativeSource AncestorType=local:WeatherBox}}"
               CommandParameter="{Binding}"
               mc:Ignorable="d">
    <base:ItemBase.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MyToolBar.Common;component/Styles/IconData.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MyToolBar.Common;component/Styles/UITemplate.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </base:ItemBase.Resources>
    <Grid>
        <!--  城市信息  -->
        <TextBlock Width="185"
                   Margin="20,20,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top">
            <Run x:Name="AreaName"
                 d:Text="Pudong"
                 FontSize="18"
                 FontWeight="Bold"
                 Text="{Binding City.Area}" />
            <LineBreak />
            <Run x:Name="CityName"
                 d:Text="ShangHai ShangHai"
                 FontSize="14"
                 Text="{Binding City.CityName}" />
            <Run FontSize="14"
                 Text="{Binding City.Province, StringFormat='  {0}'}" />
        </TextBlock>

        <!--  收藏按钮: 标准 ws:Button  -->
        <ws:Button x:Name="AddFavorBtn"
                   Width="25"
                   Height="24"
                   Margin="0,18,20,0"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Top"
                   Command="{Binding DataContext.ToggleFavorCityCommand, RelativeSource={RelativeSource AncestorType=local:WeatherBox}}"
                   CommandParameter="{Binding}"
                   CornerRadius="10"
                   Style="{StaticResource SimpleButtonStyleForWs}">
            <Path x:Name="FavorIcon"
                  Width="15"
                  ClipToBounds="True"
                  Data="{StaticResource Icon_favor}"
                  Stretch="Uniform"
                  Stroke="{DynamicResource ForeColor}"
                  StrokeThickness="2" />
        </ws:Button>

        <!--  天气图标 (字体)  -->
        <TextBlock Width="64"
                   Height="64"
                   Margin="0,0,72,0"
                   HorizontalAlignment="Right"
                   FontFamily="pack://application:,,,/MyToolBar.Plugin.BasicPackage;component/Assets/Fonts/#qweather-icons"
                   FontSize="56"
                   Text="{Binding IconChar}" />

        <!--  温度 / 天气描述  -->
        <TextBlock Margin="20,72,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   d:Text="26℃  晴"
                   FontSize="16"
                   Text="{Binding TempInfo}" />
    </Grid>
</base:ItemBase>
